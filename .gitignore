# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (local secrets / config)
.env*.local
.env

# firebase
.firebase/
firebase-debug.log*
firestore-debug.log*
*-debug.log

# editor / os
.vscode/*
!.vscode/extensions.json
.idea/
Thumbs.db

# Firebase admin SDK private keys (NEVER commit)
*adminsdk*.json
serviceAccount*.json

# MCP config (contains API key — never commit)
.mcp.json

# UI/UX design skill (local tooling, not app code)
.claude/skills/
